# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.vite/
*.tsbuildinfo

# Secrets — never commit these (CLAUDE.md §4)
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Test / coverage
coverage/
.nyc_output/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
*.swp

# Local agent state
.claude/settings.local.json

# Design tool exports — large derived bundles, not sources
design/**/.thumbnail
design/**/*Screens.html

# Playwright
playwright-report/
test-results/

# Reference documents — kept locally, not committed.
# They contain full lyrics, and this repository is public; there is no reason to
# add more copyrighted text to it than the product itself requires (R-001).
# docs/architecture.md cites them by name as the quality bar.
*.pdf
