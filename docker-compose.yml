services:
  postgres:
    # pgvector is unused by the song page, but the research pipeline needs it
    # and sharing one database is a stack decision (tech_stack.md).
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: lyricsense
      POSTGRES_PASSWORD: lyricsense
      POSTGRES_DB: lyricsense
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U lyricsense -d lyricsense']
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    volumes:
      - redisdata:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
  redisdata:
